const sinal = "Verde";

if (sinal === 'Amarelo') {
  document.write("Por favor, aguarde um pouco");
} else if (sinal === "Verde") {
  document.write("Opaa, pode passar.");
} else {
  document.write("Não pode passar");
}